# Saltglass shore after rescue

Asset: `saltglass-shore-rescued-v1.png`

SHA-256: `81638e52db015d82cdf6632febdfacac957582b4cc0b9c4d63defb6e3486974d`

Created 2026-09-13 with the built-in image generation tool by editing `saltglass-shore-arrival-v1.png`. Source illustration and its provenance remain beside this file. Both versioned PNGs are staged unchanged under the configured `campaign-art/hollow-lantern` asset directory; no live environment or running service was changed.

The companion preserves the tilted cart and rocky shore framing while removing the trapped courier. This reflects `InspectSaltglass` after rescue: the cart is empty and the courier needs an escort. The courier is outside the frame; the image does not assert their exact position, current escort or extraction route. Visual review found no remaining person, new object, label, grid or hidden location.

Display only for the trusted player view's `sceneId === 'saltglass-shore'`, `mission.packId === 'saltglass'`, and `mission.courierFreed === true`. Missing or nonboolean state must not select either state-specific illustration. Never use this as a tactical map or dungeon terrain. Exact image geometry is artistic, not authoritative movement data.

Edit prompt:

> Use case: precise-object-edit. Edit this existing 2D fantasy shore illustration into its after-rescue state. Change only this: remove the courier completely from beneath the overturned cart, leaving empty shadowed space and natural rocky sand where the person was. The rescued person is now outside the frame; do not add them elsewhere. Preserve the exact canvas dimensions, camera framing, tilted cart position and construction, visible wheel, shoreline, rocks, waves, mist, palette, lighting and hand-painted 2D ink/gouache style. Do not add blood, remains, belongings, satchels, treasure, new characters, text, grids, icons, marks or interface. The cart stays overturned; the only narrative change is that nobody is trapped underneath it anymore. Create a closely matching scene-state companion to the input.
