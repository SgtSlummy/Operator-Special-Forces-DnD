# Turn entry and private action history

This is bounded development verification, not a new full-mission or Discord acceptance run. The previously verified mission build and revision-87 evidence remain intact.

Death saving throws now resolve when a downed combatant becomes active, including the initial initiative selection. A natural 20 restores one HP before that combatant chooses an action. Stable characters do not roll again, dead characters are skipped, and End Turn never makes a second death save. The initiating actor receives only a neutral turn-ended receipt; the saving throw belongs to the downed character's private timeline. Exact command replay never advances the dice or appends history twice.

Committed command outcomes and health changes are saved atomically with state and receipts. The privateHistory projection contains only the selected character's records, even where one account owns two characters. GM projections include the full authorized timeline; public projections omit it. Result fields are allowlisted, and health-change records do not disclose an attacker's private details. Existing scoped receipts are restored into a timeline on loading older saves. The durable ledger retains all entries; projections currently return the most recent 50 per character or 100 for the GM, with recent entries shown in the Journal. Older-history pagination remains a presentation limitation.

Validation: 92 Hollow Lantern assertions and 16 legacy integration tests passed. New fixtures cover turn-entry timing, initial combat timing, natural-20 action availability, stable suppression, dead-character skipping, previous-actor receipt privacy, same-owner isolation, End Turn/replay suppression, and store restart. The focused JavaScript suite passed 55 tests; one additional public-history bridge rejection test also passed.

Build: C:/Users/Hermes/LocalFiles/HollowLanternUnity-TurnHistory/HollowLantern.exe. Unity 2021.3.14f1 build completed with exit 0; log C:/Users/Hermes/LocalFiles/hollow-unity-turn-history-build.log. Use the established bootstrap environment and launch arguments. No live process was started or stopped for this build. Full prepared-rule limitations and existing release gates still apply.

Actual Unity PlayMode regression passed one test: signed HTTP commands dispatched on Unity frames, a scene transition, bootstrap/store reload, exact receipt replay, and signed receipt lookup. Evidence: C:/Users/Hermes/LocalFiles/hollow-unity-turn-history-playmode.xml and matching .log. Death-save timing itself is covered by deterministic authority fixtures, not a new screenshot-only participant exercise. Built authority DLL SHA-256: 8DCF01EB64D706312EA475BA98EF6D7334E5EE6EAAA339C34A243DF38DA2F445.

## Fresh full-mission regression and process restart

The TurnHistory executable subsequently completed a fresh disposable mission on port 18800: 96 submissions, 95 commits, one intentional LOOT_EMPTY rejection, and no unexpected failures. All five authored routes, real attacks from the three party members and sentinel, private scouting and explicit discovery sharing, finite loot, Iona's rescue, signal repair, purchases/sales/transfer/equipment, approved short and long rests, debrief and checkpoint completed. Rescue occurred at world minute 102; repair at 124; the saved convoy outcome is convoy-guided-to-safety. These explicit synthetic-DM approvals are not automated discretionary rulings for live play.

All 480 unique public/three-character/GM images were rehashed with zero mismatches. Approved sentinel and briefing artwork is included. Evidence: C:/Users/Hermes/LocalFiles/HollowLanternMission-TurnHistory-20260909/index.html, result.json and evidence-audit.json. An old hardcoded controller build label was corrected transparently; the original walkthrough is preserved as walkthrough-original-build-label.json, and the correction is recorded in the audit. Executable process identity verifies the actual TurnHistory binary throughout.

After verifying executable path, command line, evidence log and port ownership, only this disposable process PID 19544 was stopped. A separate hidden launch restarted the same store and secret as PID 34764. The restart verifier passed at revision 95, recovered the exact original checkpoint receipt through replay and signed receipt lookup, and rendered the matching five scopes. Evidence: restart-old-process.json, restart-new-process.json, restart-verified.json and restart-player.log in the same directory.

Verdict: the new TurnHistory binary passes the bounded engine/full-mission/restart regression and is suitable for the parent's coordinated Discord cutover verification. It does not establish actual Discord presentation, independent simulated-human understanding, or completion of the remaining rule/import release gates. Keep the rehearsal store separate from the clean live campaign.
