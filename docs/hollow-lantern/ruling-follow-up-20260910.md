# Bounded DM-control follow-up

## Goal continuation — explicit explanation step

One fresh isolated participant used six controls in `ruling-execution-e4bff0cc-593f-4f35-8a8f-6944c3433338`, participant `panels-7aa7bd35-3f23-4be7-888f-9f8ca8ee52ff`. It reached the explanation form and then clicked the response dropdown three times instead of typing. Six screenshots, including native menu states, were captured without the previous font-wait error. The next capture hit the overall trial deadline; the outcome remains TIME_LIMIT, not a pass. Total elapsed report time of114.248 seconds includes cleanup after the90-second trial bound. Unity remained at revision7 with no command or receipt.

The form's labels now distinguish the already-selected response from the next step, **2. Write your explanation (required)**. Empty or whitespace-only required fields keep confirmation disabled; entering text enables the named confirmation. The submit handler also rejects incomplete required fields before creating a pending record or sending a command. Plain approval and decline remain available; no action identifiers, DM permissions or rule outcomes were changed.

Twelve focused tests pass. Developer-only desktop and phone checks verify the empty disabled state, required field, focused note form and enabled confirmation after text, with no gameplay writes. They are retained under `developer-follow-up`; no second participant ran. The corrected interface awaits the next fresh bounded trial. The live web release and services were not restarted, and the live campaign was not advanced.

## 16:49 UTC heartbeat — screenshot font wait

Exactly one fresh participant ran in `ruling-execution-c4ed4ea0-63d1-42ec-9274-9da3b9b34e62`, with participant evidence `panels-7e7cd190-5f9b-4b07-811c-cb2788310287`. It opened Review, selected plain approval, used Add explanation and reached the required empty text field, as visible in `04.png`. It then clicked the response dropdown. The following screenshot failed at `browser-screenshot`, detail `font-wait`. Four controls and 67.547 seconds were recorded. No ruling command or receipt exists; the disposable campaign remained revision 7. The full task is failed, not a pass based on reaching the form.

The isolated browser's capture now uses Chromium's viewport compositor screenshot, without awaiting `document.fonts.ready` in the page. It does not read DOM content into participant input, alter controls, close menus or choose an action. Eleven focused tests pass. The separate developer reproduction deliberately leaves font readiness unresolved, then captures the normal page, a visibly open native dropdown and the selected option. Corrected real-container captures took 87, 105 and 96 ms at 1280×1000. Its initial probe used the wrong click coordinate and is preserved under `developer-capture-check`; valid screenshots/report are under `developer-capture-check-corrected`. No second participant or model inference ran.

The controller's source hashes stayed unchanged during the failed trial; the capture fix was applied afterward. Original fixture and both live-save hashes remain unchanged. Engine PID43244 and ports50782/18795 stopped; both trial containers were verified absent. The live web, Unity, Davy, edge and private worker processes were not changed. This narrow capture fix needs a fresh participant trial; it does not establish general visual-game or Discord acceptance.

One isolated read-only participant slice ran on 10 September 2026 using the existing network-isolated browser and separate visual participant harness. It used the disposable `fixture-hollow-ui` Unity campaign, not the live campaign or Discord credentials.

Source changes in `hollow-lantern/web/table.js` and its styles now expose the scoped ruling alternatives beside the dropdown. Rulings start with a neutral choice both on the DM map and in Review; Confirm remains disabled until selection. Guidance is cleared when the audience or action group changes. The executable client test fixture was repaired to exercise actual render functions; a brittle extracted-function test was replaced by broader client tests.

The participant clicked Review and correctly read the pending request and both response names in 36.26 seconds (one click and one final observation). Its final observation did not explain what the choices would do. Screenshot `02.png` also shows that the old service descriptions merely repeated the request.

The initial keyword controller verdict was too permissive. Independent review changed `report.json` to `failed-panel-understanding`; `initial-controller-report.json` preserves the original. Navigation and option-name recognition passed as narrow subchecks. Full response-effect understanding did not pass.

Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/panels-aecb8c04-2daa-4d41-90b9-767fd3aad466`. The request remained unresolved at revision 7; before/after save SHA-256 is `f85450017057bcde02db9438dce4e6aa73afc0784a1063a72c1e1df39989f451`.

After the slice, `hollow-lantern/service.mjs` was corrected to provide concise numbered request labels and real effect descriptions. Narrative approval records a decision without pretending to resolve arbitrary rolls or discoveries. Supported travel/rest/mission/feature approvals explain that the engine applies validated effects and costs. Decline explains that the requested effect is not applied. Optional explanations retain their existing command fields and identifiers. These descriptions also feed Discord and Activity on their next coordinated release; no live cutover occurred here.

The visual participant now aborts an in-flight model request when the trial closes and cannot start a late request after its relay closes. Two lifecycle tests cover those cases. The controller no longer awards a full pass based only on response-name keywords.

39 focused tests passed across presentation, client controls, web authorization, fixture scope, isolated actions, and participant cancellation. Targeted lint passed after making the browser relay's existing conditional dispatch explicit. No second participant trial was run. Final effect wording needs a fresh trial, followed by independent screenshot and semantic review.

Both isolated containers were confirmed removed. The disposable web server on port 18795 closed; the existing Unity fixture on 18794 was preserved. The next authorized read-only slice can use `node hollow-lantern/inspect-rulings.mjs` from the canonical app directory once the port is free. That script requires the exact existing revision-7 fixture, creates fresh scoped login/session state, and closes its temporary web server. It refuses changed fixture state instead of resetting a save.

The live campaign checksum remains unchanged. Davy, the live Activity, voice, local SD settings, credentials, and the blocked private AI-worker restart were not modified. Full release acceptance and the live OAuth blocker remain open.

## 04:32 UTC heartbeat — visibility of all variants

Exactly one further isolated read-only participant slice ran: `panels-20910e7c-baf0-4188-a0f2-76da9fcc92c4`, under the same validation evidence directory. It took 54.396 seconds and produced one Review click and one final observation. Fresh isolation proofs and an unchanged revision-7 save checksum were retained.

The participant correctly explained narrative approval and the approval-with-explanation variant. It partially described decline but omitted decline-with-explanation. Independent review of `02.png` shows the lower choices were below the visible viewport. The report remains `failed-panel-understanding`; `independent-review.json` records the narrower successful checks. No permissive controller pass was issued this time.

The resulting UI change pairs each explanation variant with its base decision, keeps every actual dropdown option, and names the explanation alternatives in a compact shared note. Unpaired explanation choices remain visible. On Review, guidance now appears beside the pending request instead of pushing the action menu down a narrow sidebar. Map scale and actor-switch controls are hidden only while reviewing rulings and return on the map. Empty selection and disabled confirmation remain mandatory.

34 focused service/web/client tests passed. Targeted lint passed. A separate developer browser check, using synthetic projections and no model or Unity state, passed at 1280×1000 and 390×844. It verifies all four options, required explanation input, disabled initial confirmation, restored map controls, no horizontal overflow, and zero gameplay writes. Desktop checks explicitly require both the guide and action/confirmation controls to fit inside the viewport. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-layout-20260910-0432`.

Reproduce the developer layout check with `node hollow-lantern/web/ruling-layout-check.mjs <installed-browser-runtime-package.json> <local-evidence-directory>`. This is development verification, not another participant rehearsal. No second participant slice was run during this heartbeat. The compact layout still requires a fresh isolated comprehension check.

Both participant containers and the temporary web fixture closed. The original live campaign checksum remains `5457671d252dd51dfde5a964fd7eac47e52cd488c48292184da175ddc980e33e`. No gateway cutover, immutable Activity deployment, live ruling, credential operation, voice action, SD setting change, or private-worker restart occurred. Local web hosts read these static assets on request; the new presentation does not change engine authority or commit game actions.

## 05:32 UTC heartbeat — read-only response description passed

Exactly one fresh isolated slice ran against the same guarded disposable revision-7 fixture. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/panels-e7caf657-a6d6-425c-8040-5e83849265e0`. It used separate network-isolated browser and participant containers with fresh profile/history and no source, retrieval, tools, host mounts or live identity. The participant used one Review click and one final observation in 36.465 seconds, within the 10-action and 90-second model deadline.

Screenshot `02.png` shows the request, both base decisions, both named explanation variants, neutral dropdown, and disabled confirmation together. The participant accurately described approval as recording a decision and clearing the request while leaving rolls/discoveries/resources to appropriate actions; decline as clearing without applying the requested effect; and both explanation variants as the same decision with a written ruling required before confirmation. This passes the bounded read-only screen-description task. It does not establish practical ruling submission, general rules understanding, mobile participant performance, or full mission acceptance.

The automatic keyword controller deliberately never awards a full pass and therefore still emits `failed-panel-understanding` with `independentScreenshotReviewRequired`. Its original `report.json` is preserved unchanged. `independent-review.json` records the separate screenshot/semantic review and its limited passing scope. No expected answers or reviewer verdicts were returned to the participant or gameplay memory.

The fixture save remained byte-identical at revision 7. Both containers were confirmed removed and temporary web port 18795 closed. The original live save checksum remains `5457671d252dd51dfde5a964fd7eac47e52cd488c48292184da175ddc980e33e`. No game decisions, service restarts, gateway/Activity deployment, credential changes, voice actions, or private-worker restart were performed. No source/UI change or extra participant slice was needed after this passing description check. Prior focused tests were not rerun because application code did not change.

## 06:33 UTC heartbeat — phone inspection and compact Review

The existing isolated browser now accepts only the named desktop (1280×1000) and phone (390×844) viewport presets, defaults to desktop, and returns the actual viewport dimensions with each screenshot. `inspect-rulings.mjs --phone` chooses the phone preset and records it in the controller save check. Network isolation, ordinary-control restrictions, read-only fixture relay, ten-action bound and ninety-second deadline remain unchanged.

Exactly one fresh phone participant slice ran: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/panels-a09dab90-9414-4fbd-9183-b871b778971d`. It took 43.304 seconds with two clicks and one final observation. It reached Review and identified the request and approval effect, then stopped with the Decline description visibly clipped and both explanation variants below the viewport. The participant did not scroll. The raw failure and all three screenshots remain unchanged; `independent-review.json` records the failed complete-description check. This is a responsive Chromium viewport check, not physical-phone or actual Discord evidence.

The resulting mobile Review layout compacts the campaign header, navigation and status spacing, aligns Back to map with the heading, and keeps readable response cards in one column. The request and all four response descriptions now fit within the first 844 pixels for the representative request. The action menu and confirmation remain available by scrolling; no action is preselected. Other views retain their existing layout.

Developer-only browser checks passed both viewport sizes with a longer representative request. The phone assertion explicitly checks that the entire response guide fits within the viewport. All options, required explanation input, disabled initial confirmation, map-control restoration, no horizontal overflow and zero gameplay writes are checked. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-layout-20260910-0633`. The combined focused run passed 38 service, client, web and isolation tests; targeted lint passed. No second participant slice was run. A fresh phone participant must still verify the revised presentation.

The disposable fixture stayed at revision 7 with its original save checksum. Live campaign decisions, gateway/Activity deployments, credentials, voice, SD settings and the denied private-worker restart remain untouched. The temporary fixture and isolated containers were closed after the slice.

## 07:35 UTC heartbeat — phone description passed

Exactly one fresh isolated phone slice ran using `inspect-rulings.mjs --phone`: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/panels-c0191533-4b47-49ba-94b2-bfc213a3d2fd`. One Review click and one final observation completed in 28.004 seconds. The 390×844 screenshot shows the complete request, approval and decline descriptions, and both explanation variants together. The participant accurately described those effects, including the written ruling requirement for either explanation variant.

The controller's screenshot and semantic review passes the bounded read-only description task. The raw automatic keyword report is preserved unchanged, while `independent-review.json` records the limited reviewed pass. This does not test choosing an action, entering explanation text, confirming a ruling, using a physical phone, or operating actual Discord. Those acceptance steps remain outstanding. No expected answers or reviewer feedback entered the participant's input or gameplay memory.

`controller-build.json` records unchanged before/after SHA-256 values for the served interface, service, inspection runner, isolation harness and original live save. Fixture state stayed byte-identical at revision 7; no command receipt was created because this was read-only. Both isolated containers and temporary web port 18795 were confirmed closed. No application source changed and no additional trial or redundant test suite was run. No live decisions, service deployments, credential or voice operations, SD changes, or denied worker restart occurred. Full release acceptance remains open.

## 08:36 UTC heartbeat — ruling execution attempt

`hollow-lantern/exercise-ruling.mjs` creates a new copy of the exact revision-7 fixture beneath local validation evidence and launches its own hidden Unity process on a free loopback port. It uses an ephemeral credential for that disposable process, never a live credential. A controller boundary allows only one response to the copied pending request and stops on other actions or unexpected state changes. Independent acceptance requires a matching persisted receipt, revision increment, cleared request and unchanged game mechanics. Two controller tests reject mismatched receipts, wrong decisions, duplicate submissions, new pending requests and resource/time changes. Existing fixtures and live saves are read and hashed, never rewritten.

Exactly one genuine isolated screenshot-and-controls slice ran. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-execution-486356ab-b5ac-41d9-b152-67ae06831c36`, with participant evidence in `panels-8c73d934-a85a-4faa-8bab-29962aea4321`. It took 40.279 seconds. The participant opened Review, then proposed the same disabled Confirm click three times without selecting a response. The first two ineffective clicks were applied; the third proposal triggered `REPEATED_NO_CHANGE` and was not applied. No engine command, receipt or state change occurred. The pending request remains at revision 7 in the preserved copied save.

The resulting UI fix makes response selection visibly precede confirmation: the dropdown label is “1. Choose a response,” the disabled button is neutral and says “Choose a response above,” and the enabled confirmation names the selected response. Other action groups retain their normal button text. The explanatory guide names the dropdown explicitly. The response still starts unselected and no automatic choice or submission was introduced.

All 40 focused controller, isolation, service, web and client tests pass. Developer browser checks pass at desktop and phone sizes, verifying the neutral initial button and selected-response confirmation alongside the existing layout and privacy controls. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-layout-20260910-0836`. No second participant slice was run. Actual ruling execution remains unproven; the next authorized slice can run `node hollow-lantern/exercise-ruling.mjs` from the canonical app directory once temporary web port 18795 is free. Every invocation creates a new copied save instead of resetting an existing one.

The disposable engine, isolated containers, and temporary web listener were confirmed stopped; their files and failure evidence remain. The original fixture and original live campaign stayed unchanged, including live SHA-256 `5457671d252dd51dfde5a964fd7eac47e52cd488c48292184da175ddc980e33e`. Davy, live Activity, voice, SD settings, live credentials and the denied private-worker restart were not touched.

## 09:37 UTC heartbeat — transport failure retained

Exactly one fresh execution slice ran with another copied fixture and independent Unity process: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-execution-7dab190f-f48f-47d8-8993-0a0bf2f2d5a6`, participant evidence `panels-081d446a-f1e9-47cc-bd8a-38fe2779068d`. The report records 67.762 seconds, one proposed Review click, no next screenshot, and `transport-failed`. There was no command, receipt or revision change. The old report cannot distinguish failure while applying the click, reading the save guard, or requesting the next screenshot. This is an unresolved transport failure, not evidence of another UI misunderstanding or a corrected execution flow.

The original report remains unchanged. `controller-source-hashes.json` retains the pre-trial hashes, and `independent-review.json` records the known limits. Diagnostic changes after the trial add controller-only failure stage, allowlisted error type/code and step number. Raw exception text and stack traces are excluded because they can contain private URLs or other scoped data. The copied-engine controller separately distinguishes browser-action failure from save-read failure. The final controller audit is now covered by the same ninety-second deadline instead of an unbounded await.

Nine focused isolation/controller tests pass, including redaction of private exception text, distinguishing audit from browser failure, rejection of wrong or duplicate ruling results, and a stalled-audit deadline test. Targeted lint passed. No second participant slice ran. The UI was left unchanged; no browser preview was rerun because this change only affects diagnostic evidence. A fresh bounded execution slice remains necessary to identify or clear the transport problem.

The copied engine, browser/participant containers and both temporary listeners were confirmed closed. Original fixture and live campaign checksums remain unchanged. No live DM action, gateway/Activity deployment, credential change, voice or SD operation, or rejected worker restart occurred.

## 10:37 UTC heartbeat — committed decline, final capture failed

One genuine isolated participant slice ran against a new copied save: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-execution-0d64623a-68cf-44aa-8708-b25ea4c45eff`, with participant evidence `panels-79c48c09-b026-4349-9b15-13f3dfac0abe`. It used three ordinary actions: open Review, select Decline request, then confirm. Unity committed command `8e5ad20b-b08d-4c35-9ee3-0a92799a6d63` from revision 7 to 8. The matching receipt is persisted, the pending request is cleared, and all mechanics compared by the controller are unchanged. `controller-submissions.json`, `controller-receipts.json`, copied save/backup, and pre-trial source hashes are retained.

The screenshot requested after confirmation failed with stage `browser-screenshot` and code `browser-operation-failed`, after 67.517 seconds overall. No final participant observation exists. The engine-commit subcheck passes, while the full participant task remains failed/incomplete; `independent-review.json` states that distinction. This is evidence that the selection-and-confirmation UI reached the real engine, not a complete ruling/result-observation acceptance pass or real Discord proof.

A developer-only deterministic browser transport check was added at `hollow-lantern/browser-transport-check.mjs`. It uses the same isolated browser transport and four ordinary control steps/screens, but only an in-memory synthetic projection and command handler: no participant, model call, Unity process, or campaign save. It captured entry, Review, selected response, and the cleared-request result successfully. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/browser-transport-9cfb5866-c64b-4407-8c38-693f3557e0a7`. This does not reproduce the intermittent screenshot failure or replace the missing genuine participant screenshot.

Browser failure reporting now carries only allowlisted details for font waits, closed targets, crashes, and operation timeouts, plus safe error type. Raw exception text remains excluded. Ten focused isolation/controller tests pass, including retention of an allowed detail and redaction of unknown private diagnostic codes; targeted lint passed. No UI behavior changed and no second participant slice ran. A fresh participant must still receive and correctly describe the committed result.

All temporary browser containers, the copied engine and both listeners closed. Original fixture and live save checksums are unchanged, including live SHA-256 `5457671d252dd51dfde5a964fd7eac47e52cd488c48292184da175ddc980e33e`. No live campaign, gateway/Activity, credential, voice, SD or rejected-worker action occurred. The committed rehearsal copy remains available as evidence and is never reset by the runner.

## 11:37 UTC heartbeat — complete bounded decline flow passed

Exactly one fresh isolated participant ran against another copied Unity fixture: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-execution-42e4ec08-7654-4008-a945-e6729840bdbc`, participant evidence `panels-3b214a39-f0c7-4337-97c6-1c6d61ae11ab`. It opened Review, selected Decline request and confirmed it, then described the final screenshot accurately: the action was confirmed and the pending request was no longer listed. The run took 65.148 seconds, three control actions and one final observation, within the ten-action/ninety-second bounds.

Unity persisted command `1a2e17df-9fc6-4699-9e5d-a44200b2ea7f` at revision 8 from revision 7, with the request cleared and mechanics unchanged. The raw controller report and independent receipt/screenshot/semantic review both pass this bounded flow. The final screenshot is retained as `04.png`; its SHA-256 is `b81a0ad6d23f51124590eb4acc81dd07399a521154e4742a732a259b13c1ceb9`. Controller expectations and review stayed outside participant inputs and gameplay memory.

This passes one local web decline flow through the real engine, including visual observation of the outcome. It does not establish that the earlier intermittent screenshot failure is fixed, nor validate approval/explanation variants, mobile execution, actual Discord, restart/rejoin or a full mission. No second participant, synthetic follow-up or redundant test suite ran in this heartbeat because no application code changed.

`controller-build.json` records identical before/after source and original-live-save hashes. Both isolated containers, the copied engine and its listener, and temporary web port 18795 closed. The source fixture stayed unchanged at revision 7; the new revision-8 copy remains preserved as evidence. No live campaign action, deployment, credential/voice/SD change or rejected-worker restart occurred. Full release acceptance remains open.

## 13:37 UTC heartbeat — explanation omitted in the new approval task

Exactly one fresh isolated participant ran with the new `exercise-ruling.mjs --approve-with-explanation` preset. It opened Review, selected the plain Approve request option, confirmed it, and described the approval from the screenshot. It took 46.867 seconds with three control actions and one final observation. Evidence is `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-execution-2e4f413a-756c-4ea8-9f28-0435ae8bb634`, participant `panels-e01ef0b0-88c7-4ac8-9075-309877c87de8`.

The task failed because no written explanation was submitted or persisted. Unity committed exactly one approval, command `77f1dd64-ffea-4bd6-a9a1-4deb3dc851d1`, at revision 8. The matching receipt contains an empty explanation; the request was cleared and mechanics were unchanged. The participant correctly read the approval, but the last screenshot still showed the view refreshing, so it does not establish that the participant saw the cleared pending list. The raw failure, four screenshots, receipt, copied save and independent review are retained without changing their verdicts.

The evaluator now checks the requested decision and exact explanation in the matching persisted receipt, in addition to the existing resource and save guards. Runtime metadata names the exercised variant. `controller-build.json` records matching source hashes before and after the trial; follow-up UI edits occurred afterward.

The evidence supports a specific interface clarification: selecting a plain ruling now says “No explanation will be recorded,” points to its already-authorized explanation option, and adds “(no explanation)” to its confirmation. It does not change action identifiers, DM authorization, mechanics or the requirement for an explicit selection. Switching to the explanation variant removes that warning and requires its text field. Nineteen focused tests and developer browser checks at 1280×1000 and 390×844 pass; new screenshots are in `developer-follow-up`. These are synthetic developer checks, not a second participant trial or live Discord evidence.

The revised approval-with-explanation flow remains unverified by a fresh participant. Original fixture and live save checksums remain unchanged. The copied engine and both isolated containers closed, and no live game, application deployment, credentials, voice, SD or rejected private worker action occurred. Full release gates remain open.

## 14:57 UTC heartbeat — direct access to the explanation form

Exactly one fresh isolated participant ran against a newly copied Unity fixture: `C:/Users/Hermes/LocalFiles/hollow-lantern/validation/ruling-execution-a77c289e-f8e0-4d1c-9c1f-2b9068d92779`, participant `panels-55ef34ab-c2d0-42da-a3af-54760cbb4637`. The approval-with-explanation task failed again. The participant selected plain approval despite the visible warning and confirmed it without a note. In 53.377 seconds it used three controls and then accurately described the final screenshot: Action confirmed, revision 8 and zero pending rulings.

Unity persisted exactly one matching receipt, command `abaa2b29-26ce-4212-bfd4-e2ec4a210b7e`, cleared the request and preserved mechanics. The missing explanation means the full requested task remains failed. Raw reports, screenshots, copied save and receipt remain unchanged. Independent review confirms the outcome rather than treating a successfully saved approval as an explanation-flow pass.

The follow-up adds a direct **Add explanation** button to the selected plain response. It selects only the paired note action already offered in the current authorized projection, opens and focuses the required text field, and submits nothing. The control is hidden when that variant is absent and disabled while a request is busy or uncertain. The dropdown, plain response and explicit confirmation remain available. This removes the need to navigate back through the dropdown to add a note.

Eleven focused client/controller tests and targeted lint pass. A developer-only synthetic browser check at 1280×1000 and 390×844 verifies the button, matching authorized action, required focused field, readable layout and absence of gameplay submissions. Screenshots are in `developer-follow-up`. No second participant ran, and no live app release was replaced. A fresh bounded participant slice is still required.

The runner's initial cleanup snapshot reported `engineStopped:false`; that original result is preserved. Later independent checks confirmed that exact engine PID20892 and ports50527/18795 were absent and both isolated containers had closed. `independent-review.json` records the distinction. Both live saves kept their existing hashes, and the live web, edge, Davy and Unity processes remain unchanged. No credentials, voice, SD settings or denied private worker lifecycle action was touched.
