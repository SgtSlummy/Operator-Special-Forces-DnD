## 2026-09-11 14:33 UTC — restart fix final checks complete; not deployed

The commit-follower restart/shutdown change passed its scoped checks. The application run passed 73 tests with zero failures/skips across commit-events, commit-follower-lock, davy-host, activity-launch, app-launch and terrain-materials. Davy's hollow-lantern-config and chronicle-gateway-entry run separately passed 23 tests with zero failures/skips. These are current focused runs, not a full-application unique test total and not additional counts to add to earlier results.

New protocol-2 lock records use process-lifetime exclusive ownership, recover only validated same-host records whose owner is demonstrably absent, and archive abandoned records before replacement. Legacy/incomplete/foreign/live-owner records remain blocked. Committed results are not replayed; uncertain delivery remains pending. Closing waits for all current work, rejects late acknowledgements and returns one shared completion promise. Real disposable Windows processes exercised crashes and competing recovery. Independent development source review identified and then cleared both shutdown races. This is not a blind gameplay rehearsal.

Native Gortex change detection completed after earlier timeouts. The targeted contract initially warned about shared delivery code. After review of its impact and the actual covering tests, a scoped risk-review acknowledgement was recorded; the risk-gated contract returned `allow`. No guard rules are configured. Graph coverage did not recognize the new lock test despite the passing direct execution, and blast-radius results are marked lower bounds; no broader release certification is implied.

At 14:31:54 UTC, Davy and Activity both returned HTTP 200. Davy still reports the private game AI unavailable and autoplay not_started; port 38178 is unavailable. Both campaign saves match the preserved checkpoint bytes (rehearsal revision 79; original revision 0). No gateway restart, campaign action, private-worker restart or new Discord rehearsal occurred. The startup fix remains source-only until a controlled deployment. Automatic play still requires the private worker; its prior policy denial and pending restart authorization are unresolved. All full-mission, isolated-player, Unity candidate and presentation release gates below remain open.

## 2026-09-10 22:00 UTC — normal Activity launch restored with saved artwork

Native Gortex now resolves and reads the canonical operator-local project after the stale-cwd launcher repair. The normal app selection retains the shared rehearsal and its approved local-SD material manifest. Thirty-one focused launcher/material tests pass. The actual environment loader and material integrity check pass; no credentials were changed.

All previously supervised game listeners were absent when checked. The retained Timed engine was restored only for the paused rehearsal (PID 6784, port 18810), then `npm run app:start` started Activity (PID 21740, terminal 7835, instance 23bf1a4c-eb2f-4803-bb2c-9622b5ed2d35) using retained release 2a321757da895e9bda3b3e7b04537e5e8bb6c22c87199d4149001397cb53267a. The existing Activity edge was restored on port 18805 (terminal 63405); local and public auth-config checks returned HTTP 200 with the rehearsal identity. Both campaign saves remain byte-identical to checkpoint: rehearsal revision 79, original revision 0. No game action or candidate-engine migration occurred.

Evidence and configuration/save checkpoints: `C:/Users/Hermes/LocalFiles/hollow-lantern/activity-recovery-20260910/normal-launch-1789077464139`. Actual Discord PTB Activity verification passed for DM/Mara/Kestrel/Ash at revision 79, The Signal House, paused. Map and character/inventory/journal navigation worked, including Ash's nearby view and loading/Expand state. No commands or checkpoints were submitted; independent post-UI hashes match both original checkpoints. Evidence: `activity-recovery-20260910/normal-restart-visual-result.json` and eleven screenshots. This was enrolled-DM navigation, not isolated player privacy certification; oak was not visually observed in the stone Signal House.

Davy restoration initially failed with COMMIT_FOLLOWER_LOCKED. Its recorded old owner PID 13012 was absent; the lock was preserved, the cursor at revision 76 with pending null was retained, and only the failed owned gateway PID 20740 was stopped. Retry PID 5872 (terminal 31146) reached HTTP 200 readiness with database/cache/Discord/voice ready, Chronicle running and Hollow Lantern mounted. AI readiness correctly reports unavailable; autoplay remains not_started. Its 23 focused config/entry tests pass. Gateway evidence: `normal-launch-1789077464139/gateway-ready.json`. The original campaign engine remains stopped. No private-worker restart or alternate provider was attempted; the existing policy blocker and all unfinished release gates remain open.

## 2026-09-10 — automatic-play availability blocker

The configured private game endpoint `http://127.0.0.1:38178` refuses connections, and a listener check found no service there. The general Obus MCP service responds but is a different service and was not substituted for the isolated game route. The prior private-worker restart remains blocked by automatic approval review; no retry, replacement provider or alternate route was attempted. Davy reports autoplay `not_started`, consistent with its disabled startup flag; it currently has no external autoplay start/stop route. The rehearsal remains paused at revision 79. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/activity-rehearsal-20260910/agent-readiness.json`.

Normal app-launch persistence is being repaired independently so restarting the app selects the verified shared rehearsal rather than silently reverting to the original campaign. This does not resolve the private-agent service blocker.

## 2026-09-10 — Activity and Davy use the same rehearsal

Activity release `2a321757da895e9bda3b3e7b04537e5e8bb6c22c87199d4149001397cb53267a` now targets Davy's existing `hollow-lantern-rehearsal-20260910` authority on port 18810 and the same campaign save. Its separate Activity sign-in store preserves the original campaign's data. Signed preflight and actual Discord DM/Brother Ash views both show **The Signal House, revision 77, paused**. Neither save changed during this handoff; gateway and Unity processes were not restarted. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/activity-rehearsal-20260910/preflight.json`, `deployed.json`, and `C:/Users/Hermes/LocalFiles/hollow-lantern/native-checkpoint-20260910/activity-aligned-r77-result.json`.

A one-command actual Discord Activity checkpoint passed: revision 77→78, command `215ec582-ad87-4f6b-a641-15a06986e220`, successful persisted receipt and visible confirmation. Independent state comparison confirms unchanged mechanics and prior receipts, with one appended checkpoint history record. Evidence: `activity-rehearsal-20260910/checkpoint-controller-result.json` and `native-checkpoint-20260910/activity-checkpoint-r78-result.json` under LocalFiles/hollow-lantern. A separate native checkpoint then committed revision 78→79 exactly once (`583064c6-71d7-4f4b-9283-8c558aa2bc74`), with unchanged mechanics, earlier receipts and original save. The native overview does not display a revision number, while its successful checkpoint receipt does; Actual Discord Activity observation now also shows revision 79, The Signal House, paused, with no additional game command. Evidence: `native-checkpoint-20260910/native-parity-r79-result.json`. This proves the bounded checkpoint flow across both interfaces, not the full mission. Evidence: `activity-rehearsal-20260910/native-parity-controller-result.json`. The initial controller comparison classified the expected history append as a mechanics difference; that result is preserved separately and the corrected comparison explicitly validates the append. The original revision-zero campaign remains preserved, and the prior original-binding Activity release is retained for rollback.

## 2026-09-10 — terrain presentation checked with a disposable engine

Shaded cover barriers retain the engine's +2/+5/X markings; rubble and light/heavy fog remain clipped to their existing squares. Unknown cells and concealed entities produce no additional pixels, and remembered terrain stays dimmed. No new obstacles or rules were added.

A standalone ShopPreservation candidate loaded an archived paused revision-zero copy, enabled terrain version 1 through the existing GM command, and changed to coastal-road and signal-dungeon. Three committed receipts, eight GM/character projections and fourteen rendered maps were retained. Inspiration remained disabled, the archived source hash was unchanged, and the owned engine stopped. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/terrain-art-20260910/real-engine-d5d1cbfc-6600-4d13-9d2e-c346336fde67/result.json`. This does not deploy that engine or prove live Discord terrain gameplay. The nearby legend omission is fixed: all visible in-frame tokens now receive names and coordinates in additional rows. Hidden tokens cannot change canvas dimensions. Thirty-seven focused renderer tests and the production build pass; the retained real-engine dungeon map now lists Signal Sentinel in its second legend row.

## 2026-09-10 — actual Discord map and recovery verification

Activity release `85af14febf9af460b21b7c43e9d6b5cde2762df2fd9f7a963f7dc08dcd8deb69` passed the following native check; subsequent release `3297d19460001f246627cda093fb7fdb1d88f39e4ecffcf75ad2b131216887e2` is now live with map-loading feedback and stale-action locking, with its native display check passed: Brother Ash revision 0 showed the loading notice, then the actual nearby map and enabled Expand control, without a connection error or gameplay command. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/native-checkpoint-20260910/activity-3297-result.json`. Reopening the existing Discord Activity as the enrolled DM loaded the revision-zero paused briefing without the stale JSON-error banner. Brother Ash's scoped map completed **full 25×25 → nearby 9×9 → full 25×25**, retaining the visible own-token location at column 6, row 4 and showing the approved local-SD wood grain. This was developer-operated navigation with zero gameplay commands. The below-viewport legend was not captured. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/native-checkpoint-20260910/activity-live-recovery-result.json` and its four listed screenshots.

The recovery fix passed desktop/mobile synthetic checks for manual and automatic same-revision recovery and superseded request failures. Confirmed-action messages are retained when the following view refresh fails; desktop/mobile browser coverage confirms the receipt case, one pending-record removal, blocked stale actions and restoration after fresh state. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/activity-recovery-20260910/receipt-refresh-result.json`. The same candidate passes delayed-map loading checks in `map-loading-result.json`; these fixtures are synthetic, not live command evidence. An initial image-error branch reused a previously loaded URL and timed out (`map-error-branch-failure.json`). A corrected fresh dungeon-image request received an undecodable 503 response and passed desktop/mobile error guidance and disabled-Expand checks in `map-error-result.json`; the initial failure remains retained. The production build and focused runtime, edge, material-integrity and action-recovery tests pass. A later 33-test focused run covers the enhanced within-cell terrain art, map privacy and recovery contracts; overlapping counts are not additive. Temporary edge diagnostics are off. Davy's gateway and the Unity engines remain unchanged; original revision 0 and rehearsal revision 77 save hashes are unchanged across the Activity update.

The rehearsal's actual native Discord checkpoint committed revision 76→77 with receipt `ad2d5e98-c854-46a5-ade4-6e36077ef05d`. That closes the bounded native-DM-command gate, but not Activity command execution or a mission. It also exposed the old runtime's shop-deserialization defect: default armor stock was added during save. A separate candidate fixes this and preserves stock through checkpoint/restart/replay, but is **not deployed** because it includes the unresolved Inspiration visibility behavior. The retained rehearsal save was not rewritten. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/native-checkpoint-20260910`.

## 2026-09-10 — healing visual gate passed locally

The immutable Activity build and final Healing-Records Unity candidate passed the developer-operated copied-save UI gate: mobile allocation 8/4/3 produced HP9/5/4, spent Channel Divinity once, and showed named requested/actual healing in mobile and desktop journals. Root inspected the mobile journal and saved result. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/healing-ui-20260910/real-engine/engine-result.json`. Build hash `e738a2d4284218567b7f1cc6231df0d55118bbfe1e03de247cf18f02a7163063`; engine DLL hash `4cac577d7664b9b1d7d4e0926af6d321d703d81534b9bf296bd1c9c0f67aedea`. Fixture engine stopped and source save unchanged.

The synthetic desktop/mobile control gate also passed after fixing its malformed fake receipt; the earlier failure is explicitly reconstructed from retained tool output, not an original result copy. The first real-engine saved-state assertion mistake is retained separately. Neither fixture certifies map visuals, native Discord authentication, isolated participants or the full mission. Native allocator page retention is fixed and the root focused allocation/service suite now passes49 tests. No deployment occurred; actual Discord action-to-receipt remains the next integration gate.

## 2026-09-10 — healing candidate, not deployed

Preserve Life now accepts a validated distribution among multiple recipients through the Activity and native Discord draft controls. The engine validates the whole allocation before spending, clamps each recipient to half maximum HP, and retains requested versus actual amounts privately for the healer. Recipients receive only their own result; public events omit private amounts and undisclosed participants.

The separate Healing-Records standalone build passed actual stop/start acceptance: invalid allocation left the entire copied state unchanged, three recipients healed, one Channel Divinity use spent, duplicate command unchanged, and named private history/public events survived restart. Evidence: `C:/Users/Hermes/LocalFiles/HollowLanternUnity-Healing-Records-20260910/acceptance-39f36c88-1aad-4c4a-aed0-6d0942ae47db/result.json`. Source save unchanged, disposable engine stopped. Independent source review found no new concrete record defect. The full .NET harness passed with 56 Preserve Life assertions; the root focused app/native allocation subset passed 48 tests and production build. Counts overlap earlier runs and must not be added.

Desktop/mobile visual acceptance is in progress. The first synthetic desktop confirmation probe failed its receipt expectation; its screenshots are retained and this attempt is not a pass. Real Discord allocation submission remains unverified. Current live Unity processes still use the Timed equipment binary; Davy and Activity health checks returned HTTP 200. No healing candidate deployment occurred.

The candidate also contains undeployed Inspiration work. Hidden-target attack prompts still require resolution before release; local AI runtime policy blocks and full autonomous Discord mission, voice/scribe, isolated participant and cross-interface gates remain open. This healing evidence does not certify those gates.

## 2026-09-10 19:02 UTC — partial armor interruption deployed

Standalone Timed equipment build passed disposable Unity acceptance: invalid six-minute interruption rejected for a five-minute removal; two elapsed minutes recorded on interruption; duplicate receipt added no time; later completed removal added five minutes; equipment, AC and revision survived restart. Existing owner, stale revision and worn-transfer checks also passed. This is controller evidence, not Discord interaction or blind participant evidence.

Evidence: `C:/Users/Hermes/LocalFiles/HollowLanternUnity-Equipment-20260910-Timed/acceptance-f8dc8c8f-168a-40e4-93f1-f823536e5f31/result.json`. Build log exits successfully with code 0. Earlier focused tests: 53 armor assertions in full .NET harness and 34 focused app assertions; overlapping counts are not a unique total.

Both engines now use `HollowLanternUnity-Equipment-20260910-Timed/HollowLantern.exe`; the Activity was reloaded from the built release. Original r0 and rehearsal r76 remain paused and byte-identical to checkpoint. Deployment record: `LocalFiles/hollow-lantern/scene-design-20260910/engines-deployed.json`; prior record retained as `engines-before-timed-equipment.json`. Local web/Davy readiness and public Activity returned 200; anonymous view returned 401. Davy gateway was not restarted, so its loaded interaction adapter is not certified for the new equipment options.

Independent native Discord visual evidence now confirms legacy inventory and offered Prepare owned armor control at r0 with zero mutations: `LocalFiles/hollow-lantern/equipment-final-live-20260910/verification.json`. Firewall prompt was cleared externally. Post-conversion UI verification remains separate and pending. Ollama installer and Obus policy-denied actions were not retried. Full autonomous mission acceptance remains incomplete.
# Operation Hollow Lantern readiness

**18:52 UTC equipment engine/web deployment:** Final separate Unity build and copied-save acceptance passed. The complete .NET harness includes 40 armor assertions; root adapter/UI tests total 32. Independent review caught and verified fixes for Dash movement clamping and sheet speed disagreement. Standalone evidence: `C:/Users/Hermes/LocalFiles/HollowLanternUnity-Equipment-20260910-Final/acceptance-fa87aa16-530b-45ad-b077-1d977b7f8635/result.json`. It covers three baseline AC values, owner-scoped fighter changes, WRONG_CHARACTER/WORN_ITEM/STALE_REVISION rejections by code, body removal timing (decline zero, completion five minutes), explicit item/AC/time receipts, restart and duplicate migration prevention. Public assertion covers absence of top-level inventory only, not the full privacy gate.

Original and rehearsal engines now use the final equipment binary; web adapter rebuilt/reloaded. Both saves remain byte-identical at paused revisions 0 and 76, equipment version 0. No live conversion was submitted. The prior build and checkpoint remain available; launcher is `LocalFiles/hollow-lantern/scene-design-20260910/run-engines-equipment.mjs`. New owned equipment requires the explicitly offered GM conversion; remaining limits include wider armor categories, untrained-armor rules, partial interruption elapsed time and actual Discord equipment execution. Davy gateway itself was not reloaded in this equipment cutover.

**GPU repair policy blocker:** Same-version official Ollama 0.34.0 installer was downloaded, checksum/signature verified and checkpointed. Automatic approval review rejected its supported launch with **blocked by policy**; no installer ran and no alternate route was attempted. Existing model files and Obus remain untouched. Evidence: `LocalFiles/hollow-lantern/ollama-repair-20260910/checkpoint-20260910-115008`. Missing CUDA payload remains unresolved; this blocker is distinct from the earlier Obus worker denial.

**Latest isolated explanation rehearsal stopped before any action:** `ruling-execution-c8dccc61-1e74-46ff-a350-508a1c2132a1` captured its first screen, then the participant decision request timed out. Duration 81.400 seconds, zero actions/commands, copied revision 7 unchanged; fixture and original live save unchanged. The disposable engine stopped and no participant/browser containers or fixture listener remained. The local model status endpoint responded 200 and reported `obus-qwen3.8-27b:65k` loaded with `size_vram: 0`; this is consistent with CPU execution but does not by itself establish the timeout cause. No second participant or provider substitution was attempted. Explanation controls remain unverified by this run.

**18:25 UTC bounded image/auth retest:** Fixed confirmed transient-authorization handling: temporary 503 errors now propagate instead of being converted into false access denials. Concurrent same-owner membership checks share only pending work, with no settled authorization cache. Revocation, configuration changes, caller isolation and receipt uncertainty have focused coverage; all 37 tests and production build passed. Coordinated web reload completed. Fifteen image responses captured after reload were HTTP 200 (3.520-6.120 seconds). Actual Discord Kestrel revision 0 paused samples showed portrait and scene in all three frames across 44.652 seconds, with no game commands. Map was below viewport and is not visually certified. Temporary edge diagnostics were disabled after the sample. [Retest evidence](<C:/Users/Hermes/LocalFiles/hollow-lantern/activity-art-lifecycle-live-20260910/auth-retest-controller.json>). Earlier 403/409 and missing-image screenshots remain failures; live Discord rate limiting is not proven, and sampled success does not establish absence of flicker or full mission acceptance.

**18:02 UTC actual Discord guidance pass:** Existing services were restored after reboot; original revision 0 and rehearsal revision 76 remain paused with checkpoint-matching save hashes. Davy readiness and public Activity returned 200; anonymous private view/map/illustration returned 401. The obsolete commit-follower lock for absent pre-reboot PID 9068 was archived; cursor and saves were preserved. Restoration evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/reboot-restore-20260910.json`.

The deployed Journal/Help repair passed nine focused tests and production build. Actual native Discord verification as enrolled DM viewing Mara confirmed readable authorized location labels, distinct DM/player pause guidance, and Help naming **Recover original action** and distinguishing **Refresh**. Zero gameplay commands were submitted. [Verification](<C:/Users/Hermes/LocalFiles/hollow-lantern/activity-guidance-live-20260910/verification.json>), [Journal](<C:/Users/Hermes/LocalFiles/hollow-lantern/activity-guidance-live-20260910/journal-live.png>), [Help](<C:/Users/Hermes/LocalFiles/hollow-lantern/activity-guidance-live-20260910/help-live.png>), [DM controls](<C:/Users/Hermes/LocalFiles/hollow-lantern/activity-guidance-live-20260910/dm-controls-live.png>). This is developer-operated navigation evidence, not blind participant, mobile or full-mission acceptance. Intermittent portrait/scene blanking, inventory/AC coherence and automatic AI play remain unresolved. The private AI worker was not restarted.

**Goal continuation — explanation step clarified after a bounded timeout.** The fresh participant captured six screens, including the native response menu, without the earlier font-wait error. It reached the explanation form but repeatedly clicked the dropdown instead of entering text. The 90-second trial deadline stopped the run; total report duration including cleanup was 114.248 seconds. No command or receipt was submitted and the copied save stayed revision 7. The form now labels the selected response separately, marks the text field **2. Write your explanation (required)**, and disables confirmation until required text contains non-whitespace content. Twelve focused tests and developer desktop/phone checks pass. Evidence: `ruling-execution-e4bff0cc-593f-4f35-8a8f-6944c3433338`. This clarification still needs a fresh participant trial; the goal and full release gates remain open. Live saves and processes were preserved.

**16:49 UTC heartbeat — capture failure, no ruling committed.** One fresh isolated participant reached the approval-with-explanation form, then the next screenshot failed with `font-wait`. It used four controls in 67.547 seconds; the copied Unity save stayed at revision 7 with no submission or receipt. The browser harness now captures current viewport pixels directly from Chromium's compositor rather than waiting on page font readiness. Eleven focused tests pass. A separate developer-only real-container check captured the page, open native dropdown and selected option with deliberately unsettled font readiness in 87–105 ms; it is not a second participant trial. The initial developer probe clicked above the dropdown and is retained separately; corrected screenshots visibly show its options. Evidence: `ruling-execution-c4ed4ea0-63d1-42ec-9274-9da3b9b34e62`. Both live saves and services are unchanged, and disposable engine/containers closed. The explanation flow still needs a fresh participant run.

**14:57 UTC follow-up: explanation task still failed.** One new isolated participant again selected plain approval and omitted the requested note. Unity persisted the approval at revision 8 and the final screenshot correctly showed zero pending rulings, but the missing explanation keeps the task failed. Evidence: `ruling-execution-a77c289e-f8e0-4d1c-9c1f-2b9068d92779`. The form now offers a direct **Add explanation** button beside confirmation, selecting only the existing authorized note variant and focusing its required field. Eleven focused tests, targeted lint and developer desktop/phone checks pass; another isolated participant has not tested this change. The raw initial cleanup report said the engine had not yet stopped; subsequent process/port checks confirmed its exit and both containers closed. Both live campaign saves and services remain unchanged by this heartbeat.

**13:37 UTC approval-with-explanation check failed:** the single fresh isolated participant selected plain approval and confirmed it without a written explanation. Unity saved one approval at revision 8, cleared the request and preserved mechanics, but the requested explanation was absent. Evidence: `ruling-execution-2e4f413a-756c-4ea8-9f28-0435ae8bb634`. Plain ruling selections now explicitly warn that no explanation will be recorded and label that fact on confirmation. Nineteen focused tests and developer desktop/phone checks pass. The revised interface still requires a fresh isolated trial; no second participant ran. Original saves are unchanged and the disposable engine/containers closed. The newly running live web app and other shared services were not changed during this heartbeat.

**10 September app execution foundation:** `app:check` and `app:start` now validate the existing save and signed Unity connection, detect occupied web ports, and wait for the owned web process's HTTP readiness. Shutdown, startup cancellation, parent disconnect and late child exit have focused coverage including real disposable Vinext processes. Production build and desktop/phone entry checks passed. The app was started on loopback port 18796 against the existing paused `operation-hollow-lantern` campaign; a repeated check reused it. See [execution and recovery](app-execution.md). This does not advance a campaign, verify Discord credentials, restart shared dependencies or certify the full mission. Earlier isolated-ruling evidence below remains separate.

The first actual browser launch exposed a null-session mismatch that hid the sign-in button. The corrected entry now recognizes the real HTTP 200 `scope:null` response as signed out. Seventeen entry/auth tests, corrected desktop/phone checks, and an actual signed-out production browser check passed. Live evidence is `LocalFiles/hollow-lantern/app-execution-20260910/live-web.json`; the original failure is retained. The original campaign is still revision zero with its unchanged save checksum.

**11:37 UTC ruling flow passed:** one fresh isolated participant selected Decline, confirmed it through Unity, and correctly described the cleared request from the final screenshot. The copied save advanced 7→8 with exactly one matching persisted receipt and unchanged mechanics. This bounded local-web flow passed in 65.148 seconds with three control actions and one final observation. Evidence: `ruling-execution-42e4ec08-7654-4008-a945-e6729840bdbc`. It does not establish general capture reliability, explanation/approval flows, mobile execution, actual Discord, or a full mission. No source change or second trial was needed; original saves are unchanged and temporary services closed.

**10:37 UTC execution milestone:** a fresh isolated participant opened Review, selected Decline, and confirmed it. The copied Unity campaign committed exactly one matching receipt at revision 8, cleared the request, and preserved mechanics. The next screenshot failed, so the overall participant task remains incomplete. Evidence is `ruling-execution-0d64623a-68cf-44aa-8708-b25ea4c45eff`. A separate developer-only synthetic browser check captured all four screens, including the result; it is not participant or real-engine evidence. Safe capture-error detail reporting was expanded, and ten focused tests pass. No second participant ran. The original fixture/live saves are unchanged and disposable services closed. Visual confirmation of the committed result and full release gates remain open.

**09:37 UTC execution follow-up:** the single fresh copied-fixture trial stopped with a transport failure after the first proposed Review click, before any engine command. Precise cause remains unknown because the old report omitted the failing stage. The failure is preserved in `ruling-execution-7dab190f-f48f-47d8-8993-0a0bf2f2d5a6`. Controller reports now record safe stage/type/code markers, distinguish browser actions from save-read failures, and bound the final audit to the trial deadline. Nine focused tests pass. No second trial ran and no interface change was inferred from this transport failure. Ruling execution remains unverified; existing saves are unchanged and the copied engine/containers closed.

**08:36 UTC execution check:** one isolated participant attempted to decline a request in a newly copied fixture with its own Unity process and save. It clicked the disabled confirmation without selecting a response and stopped on repeated no-change; no command or receipt was produced. Evidence is preserved in `ruling-execution-486356ab-b5ac-41d9-b152-67ae06831c36`. The UI now labels the dropdown as the first step, uses a neutral disabled button reading “Choose a response above,” and names the chosen response on confirmation. Developer desktop/phone checks and 40 focused tests pass. The revised execution flow still needs a fresh isolated trial. Both the original fixture and live campaign are unchanged; the disposable engine and containers closed.

**07:35 UTC phone follow-up:** a fresh isolated participant passed the compact phone Review description task at 390×844, correctly describing the request, approval, decline, and both explanation variants in 28.004 seconds. Screenshot/semantic review supports that narrow result in `panels-c0191533-4b47-49ba-94b2-bfc213a3d2fd`; the conservative automatic report is preserved alongside the independent review. No ruling was selected or submitted. Source checksums, disposable revision 7 and the original live save remain unchanged. Actual action-menu use, ruling execution, physical-phone/Discord testing and the remaining full release gates stay open.

**06:33 UTC phone check:** one fresh isolated participant reached Review at 390×844 but did not scroll to the complete Decline description or explanation variants. The read-only phone task failed and its evidence is preserved in `panels-a09dab90-9414-4fbd-9183-b871b778971d`. The resulting compact phone Review layout now keeps all response descriptions within the first screen for this request; developer desktop/phone browser checks and 38 focused tests pass. A new participant check of that phone layout remains pending. No ruling or live campaign action was submitted. The harness now supports `inspect-rulings.mjs --phone` without changing isolation or bounds.

**05:32 UTC check:** the compact DM Review screen passed a fresh isolated read-only description check. The participant opened Review, identified the waiting request, and accurately described approval, decline, and both explanation variants in 36.465 seconds. Screenshot and semantic review support that narrow result; no ruling was selected or submitted. The conservative automatic report is preserved unchanged beside `independent-review.json` in `panels-e7caf657-a6d6-425c-8040-5e83849265e0`. Fixture revision 7 and the original live save are unchanged. This resolves the earlier screen-description failure below; actual ruling submission, mobile participant use, and full mission acceptance remain open. See the [bounded follow-up](ruling-follow-up-20260910.md).

**10 September follow-up:** [app foundations](app-foundations.md) now build and pass focused/local browser checks, including entry-to-table navigation; the immutable live Activity release has not changed. One new isolated DM slice opened Review and identified the waiting request and both response names, but did not explain their effects. Independent review corrected its permissive keyword verdict to failure, preserving the original. Source fixes now show every scoped response, require explicit ruling selection on the main DM map as well as Review, and explain approval/decline effects. That final wording awaits a new bounded participant slice. See the [bounded DM-control follow-up](ruling-follow-up-20260910.md). No live game decisions were advanced.

**04:32 UTC check:** one fresh read-only isolated slice understood narrative approval but omitted a response variant below the viewport; the complete task remains failed. Review now groups explanation variants, puts response guidance beside the request, and hides irrelevant map controls. All four dropdown options remain available. Developer browser checks show the entire response guide and confirmation controls within the desktop viewport, with a usable phone layout; 34 focused tests pass. The revised layout awaits the next independently scoped participant slice. No campaign decisions were submitted.

Updated 2026-09-09. **The campaign is deployed and paused; full release acceptance is still open.** The human DM is Canna's Cammera Man (Stream Acc), Discord ID1230264975533281312. The live campaign remains a fresh revision-zero briefing. Existing campaigns and saved records remain separate.

**Later rehearsal status:** the user subsequently delegated automatic decisions in the separate `hollow-lantern-rehearsal-20260910` save. All three independently scoped local AI players committed real actions through Unity and Davy, travelling from briefing to Lantern Road and the Signal House. A bounded combat continuation committed Kestrel's first attack at revision 73, then correctly stopped after a spent-action rejection. The rehearsal is paused at revision 76 with the corresponding menu/resource fixes installed; the next continuation remains unverified. The original campaign above remains unchanged. Use [the live rehearsal log](live-rehearsal.md) and its current evidence for runtime state; the older revision-zero deployment records below are historical acceptance evidence.

The sibling full-project FAIL review has been [reconciled against the current implementation](supporting-review-reconciliation.md). Its legacy authority defects still reproduce, while Hollow Lantern uses a separate live Unity authority. Two AI contract expectations and nine lab-page type errors remain; newly discovered type errors in the owned Hollow Activity page were corrected. Focused passes do not supersede the broader test, lint, dependency or acceptance gates.

## Implemented and demonstrated

Unity owns commands, resources, combat, movement, checks, conditions, concentration, reactions, travel, finite loot, transfers, shopping, equipment, rest and world time. Signed requests check membership, actor ownership, GM scope, command identity and expected revision. State, receipts, pending actions and a bounded committed-event journal are saved together. Duplicate requests return their original receipt. Presentation follows committed public snapshots and cannot independently change mechanics.

Prepared fighter Mara Flint, rogue Kestrel Vane and cleric Brother Ash have level-three sheets with resources, skills, saves, inventory, spells and feature descriptions. Enrollment supports these validated builds, custom names/background narrative, and approved compatible import records. This is not an unrestricted character builder.

Actual Davy delivery includes the public illustrated entry, private DM/character/shop spaces, grouped dropdown actions, contextual costs and confirmations, and recovery of uncertain actions. Tactical maps flatten authorized terrain and fog into PNGs; private knowledge is per character. The viewer's own visible token now has a cyan diamond and a YOU name/coordinate legend. Dungeon and regional views use discovered nodes and routes. Public attack cards show committed hit/miss/fall outcomes without private inventory or undisclosed participant information.

Local Stable Diffusion provides approved scene/guard art and terrain texture. Committed public scene changes queue new candidates; movement refreshes exact maps without regenerating art. Restart does not automatically queue artwork. Late jobs, revoked scope, and stale scenes cannot install newer-scene artwork. Previously approved assets remain usable during provider failure. Earlier portraits predate the local-SD request and are identified in their provenance records.

Music cues use the existing Davy voice player and approved licensed tracks. Loop, next and previous revalidate the approved file definition against the current campaign, scene, DM and voice scope. General music commands and the Activity Entry Point are preserved. Gain, ducking and pause behavior have automated coverage; actual voice playback and scribe acceptance are not yet certified.

## Evidence and its limits

| Evidence | Result | What it establishes |
| --- | --- | --- |
| Actual Unity PublicOutcomes rehearsal | 81 commits from82 submissions; one expected exhausted-loot rejection;410 scoped PNGs and410 projections | Complete scripted mission through the real Unity authority, all milestones, privacy projections and chronological records |
| Real Unity process restart | PID35080→9588, revision81 and original receipt recovered | Persistence across process termination and reload |
| Unity PlayMode | One test actually executed and passed in Unity2021.3.14f1 | Scene/bootstrap/frame dispatch and save/load integration; a separate zero-test filter attempt is retained and does not count |
| Actual Discord refined rehearsal |81 public cards delivered and read back; fresh-process rerun sent zero duplicates | Ordered real Discord attachment delivery and recovery, from a controller replay |
| Actual Chrome screenshot | Revision35 card visibly shows Kestrel hitting the sentinel and its fall | Native Discord desktop rendering, viewed by the developer account |
| Fresh isolated screenshot-only movement | Mara move r5→r6, correct own-token location from pixels, exact committed receipt | One ordinary-control local web slice with separate network-isolated browser and participant containers |
| Fresh rogue/cleric inspections | Correct private map identities, sheets, features and inventory items | Bounded read-only navigation slices; not a complete mission |
| DM inspection | Explicit OPEN/PAUSED and pending-ruling count implemented; final read-only Review navigation passed | An ordinary Review click opened the empty ruling state and the tester described it correctly; earlier failures remain preserved |
| Activity | Earlier token-exchange failure retained; subsequent native Discord DM authentication and scoped navigation passed | Real command/receipt, different-player privacy, cross-interface parity and mobile acceptance remain open |

Mission evidence: `C:\Users\Hermes\LocalFiles\HollowLanternMission-PublicOutcomes-20260909\index.html`, `result.json`, `restart-verified.json`, `hash-audit.json`, and `public-event-archive.json`. Build SHA-256: `A75D0DDA0E4C283CE9F33CA58E8589A8633A3643ED4340FE6A0B67C8C526A998`.

Discord record: [Hollow Lantern · refined rehearsal](https://discord.com/channels/1463393482306486387/1547375313456078848). Delivery evidence resides in `C:\Users\Hermes\LocalFiles\HollowLanternDiscordRehearsal-PublicOutcomes-20260909`. Browser screenshots and viewer/build metadata are under `LocalFiles\hollow-lantern\browser-evidence-20260909`.

Isolated participant evidence is under `LocalFiles\hollow-lantern\validation`. The successful own-token movement is `interactive-c867842a-abd4-4faf-a979-fca4f3e80e87`. Earlier expired-link, provider-timeout, coordinate, invalid-response and own-token failures remain recorded. A prematurely loose verdict was corrected to failure and its original verdict retained. These participants received screenshots and ordinary controls, not source, saves, GM packets, evaluator answers or developer memory. Controller evidence stays outside gameplay memory.

Current focused verification passed 175 application, authentication, recovery and host-control tests, and 58 Davy integration/music tests. The Obus lease-release change separately passed 57 backend tests and 2 signed HTTP cases. Production build passed. The Activity now runs immutable release `2479ec0fab8f978b21dcdf56d08f88599cc58e44edab09405579d81b9ebc064c`; six launcher tests and a real snapshot server verified stable asset delivery. An earlier in-place build failure remains preserved. Counts from overlapping focused runs must not be added as unique tests.

The final live gateway check passed all services, preserved all five private panel identities and the Activity Entry Point, and confirmed the exact original revision-zero save checksum across all five audience projections. Evidence: `C:/Users/Hermes/LocalFiles/hollow-lantern/final-live-preflight-20260909.json`.

A further populated-ruling trial did not establish full response-choice understanding. Three fresh isolated attempts correctly identified the waiting request but failed to inspect both responses. Their failures are preserved, including final trial `panels-41e3aebd-6194-4f37-947e-692c6119ba5b`. The resulting UI improvement now starts with a neutral response placeholder and disables confirmation until an explicit selection. A regression test proves that requirement; it does not replace the failed visual acceptance check.

## Remaining release gates

The hourly **Hollow Lantern rehearsal checks** heartbeat continues bounded disposable work in this task. Each run permits at most one slice, 10 participant actions and 120 seconds of model time; live DM decisions, credential rotation and the rejected worker restart are excluded. It stays quiet on unchanged blocked state and records meaningful progress or failure. Detailed participant evidence is in [interactive validation](../../raphael-council/docs/hollow-lantern/interactive-validation.md).

1. Bounded native Discord DM checkpoint is verified at revision 77 (latest evidence above). A subsequent Activity checkpoint is also verified at revision 78 (latest evidence above). Broader gameplay remains open; the earlier native checkpoint exposed the old engine shop defect, whose candidate repair is not deployed.
2. Complete a mission through actual Discord controls, plus restart and rejoin, with the live DM retaining authority. The delivered81-card rehearsal is explicitly a controller replay.
3. Complete the isolated interactive mission for independently scoped player and DM testers, including screenshot-only terrain, fog, discovery and transition checks. Current successful slices do not establish this whole journey.
4. Complete real-account Discord Activity identity, cookie, private-map and receipt checks against the same authority, then test switching between Discord and the Activity. The 15:12 UTC application check confirms the existing `/activity` redirect is registered, Embedded is enabled, the global Activity launch command exists, and public configuration selects `operation-hollow-lantern`. The missing `/api/auth/discord/callback` registration is a separate standalone-browser sign-in gate, not a prerequisite for launching the Discord Activity. Replacement credentials passed independent account/application and client-credentials checks, and subsequent native Discord relaunches successfully authenticated the enrolled DM. Command/receipt execution, private-map delivery under different player identities, and cross-interface state parity remain unverified. The authorized 14:51 UTC reload preserved the original web campaign and separate paused Davy rehearsal. Evidence: `C:\Users\Hermes\LocalFiles\hollow-lantern\discord-credential-setup\activity-entry-verification.json` and `credential-reload-20260910\http-verification.json`. Test mobile before enabling its portal support flags.
5. Demonstrate actual music playback, narration ducking, pause silence, consent-based voice/scribe capture, corrections and scoped recall in the game voice channel.
6. Deploy and verify the signed AI master-lease release endpoint. Clean-shutdown source and tests are ready, but the private worker still runs its previous version after automatic approval review rejected its restart with “blocked by policy.” The current compatibility fallback preserves active-host protection; rapid restart may require the old30-second lease to expire.
7. Finish remaining rule variants or explicitly mediate them: general Heroic Inspiration rerolls, Alert swaps, full multi-target Preserve Life, arbitrary armor/build variants and populated general import approval workflows. Do not infer unsupported effects from feature names.
8. Verify final runtime lifecycle and recovery from the chosen launch method. Current foreground supervision and persistent Tailscale forwarding are documented; Windows login auto-start has not been installed.
9. Resolve or explicitly disposition the remaining full-project review findings under their existing owners, including legacy authority defects, generated-asset restoration, provider shutdown deadlines, contract expectations, type/lint failures and dependency advisories. Preserve the distinction between the legacy host and the live Hollow authority.

The [illustrated DM packet](dm-packet.html), [party sheets](prepared-party.json), [Unity launch contract](unity-live-launch-contract.md), [Activity recovery](activity-recovery.md), and [deployment recovery](deployment-preflight.md) are ready for review. The local SD checkpoint was reloaded after isolated vision testing. A fresh signed disposable image request succeeded (candidate57fc3b31-b54d-44cf-b716-31855539437a, model6ce0161689, SHA52363e05d2ac87ec4fb0e30c2531c56bdcd608a241c8a4cded10f44a3f696f66), but its modern-looking interior was not selected for deployment. This report is the remaining-work record, not a release certificate.

