# Current acceptance status

Last updated: 2026-09-09 UTC

## Fresh local evidence

- The user reconfirmed `C:\Users\Hermes\Projects` as the final project destination. The already-local canonical Obus runtime remains `C:\Users\Hermes\Documents\obus-moa-exe`; backups remain under LocalFiles. All 170 previously recorded migration destinations exist and their recorded source paths remain absent. A fresh audit found a newly created ComfyUI sidecar in the old Operator folder and a new Operator DND instructions file. The sidecar's 2,190 files (240,306,263 bytes) were backed up, moved into the canonical Operator `podman` folder, and SHA-256 verified in both backup and destination. The instructions file was also backed up and hash-verified after moving. ComfyUI was stopped only after its queue was empty, then restarted with its exact image and settings using six local Projects bind mounts. Its queue API responds and it has zero OneDrive mounts. The seven audited old project roots now contain zero files. New evidence is under `C:\Users\Hermes\LocalFiles\MigrationReports\20260909-comfyui-projects`; earlier evidence remains under `20260908-projects-final`. Native Codex saved project paths remain unresolved.
- Obus source and installed plugin launchers reference the canonical local runtime. Fresh stdio startup and nine mocked connector routes passed.
- Private game launcher endpoint selection, token handling, ownership checks and duplicate prevention pass seven mocked PowerShell fixture groups. `.env.local` selects port `38176`.
- Game/backend/STT/retrieval/provider/evidence regression suite: 343 tests and 14 subtests passed, including 42 retrieval cases, 27 receipt-commit race cases, 105 strict provider cases, 115 evidence/API cases and 25 synchronization-runtime cases. Operator-to-Obus offline contract: 22 tests passed, including guards against captured provider defaults reaching HTTP and SQLite URI writes escaping temporary fixture storage. These use controlled providers and do not prove live game inference.
- Chronicle now exposes complete, versioned evidence projections with participant consent epochs, original/corrector lineage, stable source references and correction/deletion revisions. The final combined host-control, signed-client, bridge and projection run passes 98 tests, including seven new projection fixtures. Eight existing focused privacy/projection fixtures also passed. A temporary cross-language fixture sent an actual corrected Chronicle projection through Obus validation/storage/resolution: both contributors were preserved, only corrected text was returned, and external-consent withdrawal denied external resolution. No network or live campaign stores were used.
- Obus accepts host-signed, runtime-fenced evidence snapshots and resolves bounded reference jobs from a dedicated campaign/session store. Snapshot/source/consent version checks reject rollback, conflicting revisions, stale derivations, unauthorized sources and revived consent epochs. Reference jobs revalidate before dispatch, replay and the guarded receipt commit; a correction during generation cannot save an outdated result. The snapshot limit is 2,048 sources / 256 participants / 512 KiB; reference closure is limited to 32 sources / 16,000 text characters. Requests exceeding bounds fail without truncation.
- One actual local inference request passed through the canonical Obus game adapter and in-process private FastAPI route: HTTP 200 in 18.194 seconds, `obus-qwen3.8-27b:65k`, `key-local-ollama`, first attempt, local destination. The request contained only a synthetic green-beacon fact; the real catalogue and strict Ollama transport were used. Temporary game/runtime stores and a temporary service token were removed afterward. This proves real adapter inference, not production endpoint availability or multiplayer acceptance.
- Signed evidence synchronization now has its own fenced transaction guard, allowing consent/correction updates while AI is disabled. Generation and inference receipts still require enabled AI. Live lease, generation, policy revision, transaction ownership and local-only checks remain enforced.
- Text and STT receipts, including legacy STT normalization, now commit under the runtime SQLite writer lock. Separate-process fixtures cover DELETE/WAL combinations, policy changes during inference, rollback, lease expiry and stale generations. Inference does not hold that lock.
- Local text generation now verifies completion-capable local GGUF metadata before sending evidence, normalizes `localhost:11434` to fixed `127.0.0.1:11434`, and rejects cloud-backed models, tool output and model mismatch. A fresh metadata-only check of `obus-qwen3.8-27b:65k` reported GGUF, `qwen35`, completion capability and no top-level remote metadata; no inference was performed.
- Chronicle lifecycle/consent/queue tests: 42 passed. Shared web/Activity/auth tests: 10 passed, including GM actions in either interface and revocation affecting both existing GM sessions.
- The Obus speech decoder now consumes audio from memory and closes the stream after success or failure. Actual local `faster-whisper-tiny` decoded one 4.259-second synthetic utterance in 1.436 seconds including model initialization. No audio files, network calls, or participant capture occurred. Noisy and multi-speaker accuracy remains untested.

## Previously recorded evidence

Tactical/world/council, Discord, character, imagery, storyboard/music, host and recovery suites passed at their recorded scopes. The local build, dev `/play` serving and isolated production `/play` serving were previously verified; they were not rerun for this speech/launcher change. Local host-control and deployment secrets exist and must remain private.

## Current runtime and implementation limits

- The private game endpoint on `38176` was unreachable in the latest diagnostics. Starting the repaired launcher was rejected by command policy with only “blocked by policy”; no process was started and no alternate launch was attempted. The existing Obus core remains separate. Do not interpret the earlier running-service observation as current readiness.
- Free-provider and Codex dispatch remain disabled by backend runtime and route checks. The game-specific free transport is implemented for an explicitly pinned OpenRouter `:free` model and one downstream provider: it disables fallback/tools, applies zero-price ceilings and requires matching response provider/model plus zero, non-BYOK cost. The dormant route consumes validated structured provenance and host pins. No free-provider credentials were configured and no external inference was run.
- The Operator default Chronicle runtime now composes the signed evidence bridge with the Obus provider. Raw-source summaries use exact versioned references; condensed chapters and final prose remain inline and local. An independent single-flight scheduler persists acknowledgements and retries, synchronizes corrections/deletions/consent even for ended sessions, refreshes every 30 seconds and resynchronizes after process restart. Signed synchronization works while AI is disabled. The latest combined runtime/provider/projection/migration/composition suite passes 171 tests; 13 exercise actual service composition. Schema changes create a consistent local backup before migration; only temporary fixture databases were migrated.
- Shared voice capture is now extracted into an import-time inert receiver. Davy's adapter reuses the existing music-owned player and capture lease, refuses a conflicting music channel, checks current authorization before decoding and bounds/erases audio buffers. The core exposes a terminal, retryable `stopCapture()` that prevents later queued commands from restarting capture while accepted messages drain. A fresh combined Chronicle/voice/auth run passes 208 top-level tests (the two standalone wrappers also execute 16 nested cases); Davy's actual-player adapter fixtures pass 9 tests. The shared Discord authority helper preserves existing browser/Activity GM-role withdrawal behavior without promoting saved players. The portable host factory passes 10 tests; its package build/install audit passes 3 tests, including 11 isolated runtime fixtures. The final bundle includes the shared authorization policy and sealed capture shutdown.
- Davy's actual configuration module now includes read-only Chronicle identity checks before writable database constructors, immutable shared browser/Activity Discord bindings, lazy credentials and collision checks before upserting the known `/session` command. Its 12 configuration fixtures pass. `raphael-council/packages/chronicle/pending-davy-config.patch` is marked superseded and must not be applied. Current setup requirements are documented in `raphael-council/packages/chronicle/DAVY_HOST.md`.
- Davy's production entry point now mounts the configured Chronicle onto the existing Discord client and music-owned voice player, registers commands after login, reports safe lifecycle status, and closes Chronicle before destroying the voice/client infrastructure. Capture is terminally stopped before accepted work drains. Startup failures during mounting, login, registration, guild synchronization and subscriber startup clean up Chronicle. The fresh combined suite passes 45 tests: 16 lifecycle, 12 configuration, 9 actual-player voice and 8 isolated tests of the actual gateway entry point. The entry-point fixture rejects unknown dependencies and intercepts all infrastructure imports; it does not start live services. Syntax and scoped whitespace checks pass.
- Gortex initially timed out before applying Davy corrections. Fresh guarded symbol edits and later ordinary file edits succeeded; the corrections above are physically committed. The final main-module batches report a fresh graph, detection returns the changed main symbols, and their contract verdict allows the behavioral change with lower-bound coverage. Some broader config/test graph checks still time out. The existing seven-container Davy deployment was observed using canonical Projects paths and was not restarted or changed. Chronicle has not been enabled in that deployment, no live campaign database was migrated, and no real Discord audio was captured.
- External routing remains disabled. Before enabling it, finish authoritative host-policy intersection, classified requester input, external dispatch/cancellation records, restoration and large-session paging. Privacy synchronization must also remain possible after the original host loses membership. A changed full-snapshot revision currently invalidates pending reference jobs; summary throughput during continuous capture remains unverified. Passing an exportable boolean is insufficient authorization.
- Gortex physical edits are verified, but some graph impact/detection remains incomplete because indexing receipts were pending or failed. Fixture results establish only their tested behaviors; an empty graph coverage or allow result does not establish complete impact coverage. The current 208-test Chronicle/voice/auth run includes both standalone voice wrappers and their 16 nested cases.
- Codex still needs a verified tool-free inference transport. The existing coding adapter permits tools and workspace writes and must remain separate from gameplay. Full-vector indexing, end-to-end deadlines/cancellation and live route acceptance also remain outstanding.
- Campaign-scoped semantic ranking is integrated behind `OBUS_GAME_EMBEDDING_MODEL` and remains opt-in. SQL authorization and tombstone filtering precede bounded candidate selection and local embedding. The helper verifies local model metadata, uses only fixed loopback Ollama, rejects redirects/cloud models, bounds requests/cache/deadlines and falls back to lexical matches. Corrections, audience changes and model-weight changes invalidate cached vectors. This is bounded candidate reranking, not a complete persistent vector index.
- A pre-final helper benchmark of installed `nomic-embed-text:latest` verified local GGUF/768-dimensional embeddings. The first request hit the four-second deadline; later requests found both expected sources without keyword overlap (about 2.99 seconds uncached and 40.5 milliseconds cached). The helper changed during that benchmark, so it is not verification of the final source bytes or a production-ready profile. Production enablement and full-campaign relevance/startup testing remain outstanding.
- Codex still displays legacy OneDrive paths for some saved projects/tasks. The files are local, but supported app rebinding remains unresolved; do not rewrite live app state or promise that restart alone repairs it.

## Remaining live acceptance

A GM and two participating accounts must finish an authored mission across Discord Activity and web, with real Discord voice, consent withdrawal/corrections, scoped retrieval, a controlled restart and matching authoritative state. Repeat key scenarios with local-only AI, allowed free routes, eligible Codex enabled and all AI unavailable. Credentials, approved campaign material and a stable routine-hosting domain remain external inputs. Illustrative image generation is optional; existing assets and deterministic maps remain usable.

Synthetic fixtures and speech benchmarks are diagnostic evidence, not completion of the authored multiplayer mission or release acceptance.
